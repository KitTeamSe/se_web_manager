server {
	listen 3001;

	location / {

		if ($http_origin ~ '^https?://(localhost|YOUR_API_DOMAIN_URL)') {
		  add_header Access-Control-Allow-Origin $http_origin;
		}

		root /usr/share/nginx/html;

		index index.html index.htm;

		try_files $uri  $uri/ /index.html;

	}
}