import React from 'react';
import styled from 'styled-components';
import Header from '../../modules/Header/Header';

const Wrapper = styled.div``;

const AuthGroupListView = () => {
  return (
    <Wrapper>
      <Header title="권한그룹 관리" />
    </Wrapper>
  );
};

export default AuthGroupListView;
